"""
External tool runners for mosaic.

Scripts in this directory bridge mosaic with external packages that have
incompatible dependencies or restrictive licenses. They are invoked via
subprocess using a separate Python environment.
"""
