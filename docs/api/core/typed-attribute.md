# Typed attributes

The grammar behind a dataset's tags: six types, the constraints each one allows,
and the values that satisfy them. mosaic-api speaks the same grammar for the tags
a user attaches to a sequence or an individual, so a tag means one thing wherever
it is written.

::: mosaic.core.typed_attribute
    options:
      show_source: true
      members_order: source
